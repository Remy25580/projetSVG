#ifndef COLOR
#define COLOR
/**
 * Set the color for the styling of a shape
 * @param s object of the structure shapeStyle, which contains the color
 * @param fillOrContour define if the contour color is defined (0) or the filling color (1)
 */
void setColor(shapeStyle *s, int fillOrContour);

#endif